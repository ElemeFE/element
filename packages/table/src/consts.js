export const TABLE_ID_SEED = 1;

// 默认的表格内容高度
export const DEFAULT_TABLE_BODY_HEIGHT = 20;

// 默认的最大单页数据条数
export const DEFAULT_MAX_PAGE_SIZE = 50;

// 默认的表格行高
export const DEFAULT_BODY_TR_HEIGHT = 44;

// 复用时的最小数据量
export const MIN_DATA_QUANTITY = 200;
