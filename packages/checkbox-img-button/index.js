import CheckboxImgButton from './src/main';

/* istanbul ignore next */
CheckboxImgButton.install = function(Vue) {
  Vue.component(CheckboxImgButton.name, CheckboxImgButton);
};

export default CheckboxImgButton;
