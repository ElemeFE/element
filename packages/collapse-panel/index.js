import ElCollapsePanel from '../collapse/src/collapse-panel.vue';

/* istanbul ignore next */
ElCollapsePanel.install = function(Vue) {
  Vue.component(ElCollapsePanel.name, ElCollapsePanel);
};

export default ElCollapsePanel;
